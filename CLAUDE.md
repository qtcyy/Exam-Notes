# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库性质

这是一个 **Obsidian 知识库**（Vault），专门用于存放**考研（全国硕士研究生统一招生考试）复习资料**。本仓库不是代码项目——没有构建、测试、lint 等开发流程。

## 目录结构

```
/
├── 欢迎.md              # Obsidian 默认欢迎页（可删除）
├── 计算机网络/           # 按科目分类的笔记目录
│   ├── 图片文件/         # 该科目的附件（图片、截图等）
│   └── *.md             # 章节笔记
└── .obsidian/           # Obsidian 配置（勿手动修改）
```

新科目按同样模式建立：`科目名/` 目录下放笔记，图片等附件放在 `科目名/图片文件/` 或统一附件目录中。

## Obsidian 特有语法

笔记中使用的 Obsidian 扩展 Markdown 语法：

- **Wiki 链接**：`[[页面名]]` 或 `[[页面名|显示文本]]` —— 链接到库内其他笔记
- **图片嵌入**：`![[图片名.png]]` —— 嵌入附件图片
- **LaTeX 数学公式**：`$行内公式$` 或 `$$块级公式$$`
- **属性/Frontmatter**：YAML frontmatter 用于标记笔记元数据（标签、日期等）

## 笔记编写约定

- 每篇笔记对应一章或一个知识点模块
- 标题层级：`##` 用于大节，`###` 用于小节，`####` 用于具体知识点
- 数学符号使用 LaTeX 格式
- 图片统一存放在对应科目目录的 `图片文件/` 子目录中
- 跨科目引用使用 Obsidian Wiki 链接语法

## 当前内容

| 科目 | 已有内容 |
|------|---------|
| 计算机网络 | 第三章 数据链路层（流量控制、停止等待协议、滑动窗口机制） |

## Claude 在此仓库的工作方式

当协助处理此仓库时，Claude 的角色是：

1. **笔记整理与格式化**：将原始笔记整理为结构清晰的 Markdown，确保层级合理、LaTeX 公式正确
2. **知识点补充**：根据教材知识补充笔记内容，保持与已有笔记风格一致
3. **笔记审查**：检查笔记中的概念错误、遗漏、表述不清之处
4. **跨科目关联**：发现知识点间的关联时，使用 Obsidian Wiki 链接建立连接
5. **目录维护**：随着内容增多，帮助维护索引/目录笔记
